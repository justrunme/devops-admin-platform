image:
  repository: ghcr.io/justrunme/devops-admin-platform/ui
  tag: latest

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  hosts:
    - host: devops.local
      paths:
        - path: /
          pathType: Prefix
